apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  - service.yaml
  - deployment.yaml
  # Secret is NOT included — apply manually from secret.yaml.template

namespace: default

images:
  - name: ghcr.io/thiscloud/ia-orquestador
    newTag: latest
